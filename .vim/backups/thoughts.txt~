- fee none--n/a and back in fstruct form
- 

field:
param multi/single
generate choices on init based on data
strip prefixes, non-model
ignore self choices
if invalid - get all


all forms:
-

filtertable
-- generates list of fields for form
-- get_queryset (move from form?)
-- table headers etc

forms:
filtertableform
--fields passed in. may be fk or non fk
--multichoice
--needs sort field
--fields need strip sort (ie non-model)
--clean with mutually incompatible choices


feecalc:
--singlechoice
--fields need strip prefixes and quantity, and any other non-model (mgmt form)
--needs to update choices (ignore self choices)
--data needs all
--needs find_fee

feecomp
--as above
--no chamber
--find_all_matching_fees

ajax dropdowns:
-- strip prefixes and non-model
-- no need for data cleaning
-- no need to store data
-- logic for ignoring choices on self shared


best place for strip_prefixes etc?  on form, not on field, cos duplication
then field does one op to remove self
