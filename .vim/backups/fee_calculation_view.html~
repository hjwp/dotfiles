{% extends "pricing/base_calculators.html" %}

{% block content %}
  
 <h2>Invoice Calculation Details</h2>
    
    <table width="90%" border="1" summary="detailed line items of fee comparison">
    	<thead>
	      <tr>
			<th>Chamber/Firm</th>
			<th>Fee Type</th>
			<th>Framework Panel</th>
			<th>Level of Court</th>
			<th>Type of Work</th>
			<th>Seniority</th>
			<th>Quantity</th>
			<th>Unit Fee</th>
			<th>Subtotal</th>
	      </tr>
	  	</thead>

      	<tbody>
	  {%if lines %}
		{% for line in lines %}
		  <tr class="{% cycle 'row1' 'row2' %}">
		    <td><a 	title="click for firm / chamber's relevant barristers" 
					href="{% url chamber_for_invoice chamber_id=line.fee.chamber.id feecalc_id=feecalc.id %}">
				{{line.fee.chamber}}</a>
			</td>		  	
		    <td>{{line.fee.fee_type}}</td>
		    <td>{{line.fee.framework_panel}}</td>
		    <td>{{line.fee.level_of_court}}</td>
		    <td>{{line.fee.type_of_work}}</td>
		    <td><a 	title="click for firm / chamber's relevant barristers" 
					href="{% url chamber_for_invoice chamber_id=line.fee.chamber.id feecalc_id=feecalc.id %}">
				{{line.fee.seniority}}</a></td>
		    <td>{{line.quantity}}</td>
			<td>{{line.fee.fee}}</td>
			<td>{{line.line_total|currency}}</td>
		  </tr>
		{% endfor %}
		
      {% endif %}
	  <tr>
	  	<td colspan = "8">Overall Total</td>
		<td>{{feecalc.overall_total|currency}}</td>
	  </tr>
	  </tbody>
    </table>
    {% if related_comparison %}
		<p>This invoice calculation is part of a fee comparison called: 
			<a href="{% url pricing.views.fee_comparison_view related_comparison.id %}">
				{{related_comparison.name}}
			</a>
		</p>
	{% endif %} 
{% endblock content %}
